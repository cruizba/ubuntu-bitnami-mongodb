services:
  mongodb:
    image: ${MONGODB_IMAGE:-cruizba/ubuntu-bitnami-mongodb:8.2}
    environment:
      - MONGODB_ROOT_PASSWORD=rootpass123
      - MONGODB_USERNAME=testuser
      - MONGODB_PASSWORD=testpass123
      - MONGODB_DATABASE=testdb
    ports:
      - "27017"
